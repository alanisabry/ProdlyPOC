<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Not_allowed_to_change_Source_fields</fullName>
    <active>true</active>
    <description>checks if the user is trying to change the Lead Source and Source field. This is not allowed if a value has already been previously populated - BODEV 4100</description>
    <errorConditionFormula>AND(NOT(CONTAINS($Profile.Name,&apos;System Administrator&apos;)),
OR(
AND(
ISCHANGED(LeadSource),
NOT(ISBLANK(PRIORVALUE( LeadSource )))
),
AND(
ISCHANGED(Source__c ),
NOT(ISBLANK(PRIORVALUE(Source__c )))
)
)
)</errorConditionFormula>
    <errorMessage>It is not allowed to change the Lead Source or Source values.</errorMessage>
</ValidationRule>
