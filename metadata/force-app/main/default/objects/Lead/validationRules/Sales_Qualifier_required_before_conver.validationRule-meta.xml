<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Sales_Qualifier_required_before_conver</fullName>
    <active>false</active>
    <description>Before converting a lead, a user must be added to the Sales Qualifier field.</description>
    <errorConditionFormula>AND(
IsConverted,
ISPICKVAL(Source__c, &quot;Outbound&quot;),  
ISBLANK(Sales_Qualifier_New__c))</errorConditionFormula>
    <errorMessage>Please add a user to the Sales Qualifier field before converting the lead.</errorMessage>
</ValidationRule>
