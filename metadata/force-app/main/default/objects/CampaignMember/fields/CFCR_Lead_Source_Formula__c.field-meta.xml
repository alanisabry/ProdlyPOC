<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CFCR_Lead_Source_Formula__c</fullName>
    <description>wd field
displays lead source of related lead/contact. used for reporting because the campaign member field is not available in campaign member field reports.</description>
    <externalId>false</externalId>
    <formula>IF( NOT(ISBLANK(ContactId)), text( Contact.LeadSource), 
text(Lead.LeadSource))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>displays lead source of related lead/contact. used for reporting because the campaign member field is not available in campaign member field reports.</inlineHelpText>
    <label>Lead Source</label>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
