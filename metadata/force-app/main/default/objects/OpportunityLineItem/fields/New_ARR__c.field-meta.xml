<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>New_ARR__c</fullName>
    <description>Formula returns value for New ARR when an opportunity is closed won.</description>
    <formula>IF(
  AND(
    For_Renewal__c = TRUE,
    ISPICKVAL(Opportunity.Type, &quot;New Business-Enterprise Cloud&quot;),
    ISPICKVAL(Opportunity.StageName, &quot;Closed Won&quot;),
    ISBLANK( TEXT(Opportunity.Account.OEM__c) ) /* Except OEM */
  ),
  Total_Sales_Price_ARR__c,
  NULL
)</formula>
    <label>New ARR</label>
    <precision>18</precision>
    <required>false</required>
    <scale>8</scale>
    <type>Currency</type>
</CustomField>
