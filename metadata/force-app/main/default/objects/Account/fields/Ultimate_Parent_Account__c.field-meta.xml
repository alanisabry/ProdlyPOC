<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Ultimate_Parent_Account__c</fullName>
    <description>BODEV-4287 New Field: Ultimate Parent Account Field on Account</description>
    <externalId>false</externalId>
    <formula>HYPERLINK(
    IF(
        ISBLANK(ParentId), 
        Id, 
        IF(
            ISBLANK(Parent.ParentId), 
            Parent.Id, 
            IF(
                ISBLANK(Parent.Parent.ParentId), 
                Parent.Parent.Id, 
                IF(
                    ISBLANK(Parent.Parent.Parent.ParentId), 
                    Parent.Parent.Parent.Id, 
                    Parent.Parent.Parent.Parent.Id
                )
            )
        )
    ),
    IF(
        ISBLANK(ParentId), 
        Name, 
        IF(
            ISBLANK(Parent.ParentId), 
            Parent.Name, 
            IF(
                ISBLANK(Parent.Parent.ParentId), 
                Parent.Parent.Name, 
                IF(
                    ISBLANK(Parent.Parent.Parent.ParentId), 
                    Parent.Parent.Parent.Name, 
                    Parent.Parent.Parent.Parent.Name
                )
            )
        )
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Ultimate Parent Account</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
